"use client"

import { RouteError } from "../../../components/ui/route-error"

export default function DashboardAlertsError() {
  return <RouteError message="We couldn't render the alerts page right now." />
}
