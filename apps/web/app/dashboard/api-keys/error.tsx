"use client"

import { RouteError } from "../../../components/ui/route-error"

export default function DashboardApiKeysError() {
  return <RouteError message="We couldn't render the API keys page right now." />
}
