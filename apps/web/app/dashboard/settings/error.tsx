"use client"

import { RouteError } from "../../../components/ui/route-error"

export default function DashboardSettingsError() {
  return <RouteError message="We couldn't render the settings page right now." />
}
